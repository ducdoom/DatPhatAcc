<UserControl
    x:Class="DatPhatAcc.Views.CreateSaleInvoiceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DatPhatAcc.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:DatPhatAcc.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:CreateSaleInvoiceViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <DockPanel>
            <Button Command="{Binding LoadXmlFileCommand}" Content="Chọn file" Width="Auto" Style="{StaticResource PrimaryButton}"></Button>
            <TextBox Text="" Style="{StaticResource PrimaryTextBox}"></TextBox>
        </DockPanel>

    </Grid>
</UserControl>
