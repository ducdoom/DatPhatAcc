<UserControl
    x:Class="DatPhatAcc.Views.SyncPurchaseView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DatPhatAcc.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:DatPhatAcc.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:SyncPurchaseViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding Customers}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Binding="{Binding CustomerId}"
                    Header="ID"
                    IsReadOnly="True" />
                <DataGridTextColumn
                    Binding="{Binding CustomerName}"
                    Header="Name"
                    IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <!--<Button Command="{Binding LoadCustomersCommand}" Content="Load cus" />-->
    </Grid>
</UserControl>
