<UserControl
    x:Class="DatPhatAcc.Views.SyncPurchaseView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
    xmlns:local="clr-namespace:DatPhatAcc.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:DatPhatAcc.ViewModels"
    d:DataContext="{d:DesignInstance Type={x:Type viewmodels:SyncPurchaseViewModel}}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="White"
    
    mc:Ignorable="d">



    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <StackPanel
                    Grid.Row="0"
                    Margin="3"
                    Orientation="Horizontal">

                    <StackPanel.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center" />
                        </Style>
                    </StackPanel.Resources>

                    <TextBlock Text="Từ ngày:" />
                    <dxe:DateEdit DateTime="{Binding FromDate}" />
                    <TextBlock Text="Đến ngày:" />
                    <dxe:DateEdit DateTime="{Binding ToDate}" />
                    <Button Command="{Binding SearchTransactionOverviewCommand}" Content="Tìm kiếm" />
                </StackPanel>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <GroupBox Grid.Column="0" Header="Tìm kiếm NCC">
                        <dxg:GridControl
                            ItemsSource="{Binding Customers}"
                            SelectedItems="{Binding SelectedCustomers}"
                            SelectionMode="MultipleRow">

                            <dxg:GridControl.View>
                                <dxg:TableView ShowCheckBoxSelectorColumn="True" />
                            </dxg:GridControl.View>
                            <dxg:GridColumn
                                AllowEditing="False"
                                FieldName="CustomerId"
                                Header="Mã NCC" />
                            <dxg:GridColumn
                                AllowEditing="False"
                                FieldName="TaxCode"
                                Header="MST" />
                            <dxg:GridColumn
                                AllowEditing="False"
                                FieldName="CustomerName"
                                Header="Tên NCC" />
                        </dxg:GridControl>
                    </GroupBox>

                    <GroupBox Grid.Column="1" Header="Phiếu nhập">
                        <dxg:GridControl ItemsSource="{Binding TransactionOverviews}" SelectionMode="MultipleRow">
                            <dxg:GridControl.View>
                                <dxg:TableView
                                    ShowAutoFilterRow="False"
                                    ShowCheckBoxSelectorColumn="True"
                                    ShowGroupPanel="False"
                                    ShowSearchPanelMode="Never" />
                            </dxg:GridControl.View>
                            <dxg:GridColumn
                                AllowEditing="False"
                                FieldName="TransactionDate"
                                Header="Ngày tháng" />
                            <dxg:GridColumn
                                AllowEditing="False"
                                FieldName="TotalPriceVat"
                                Header="Số tiền">
                                <dxg:GridColumn.EditSettings>
                                    <dxe:TextEditSettings DisplayFormat="N0" />
                                </dxg:GridColumn.EditSettings>
                            </dxg:GridColumn>
                        </dxg:GridControl>
                    </GroupBox>
                </Grid>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>



            <Button Grid.Column="1" Content="Test" />
        </Grid>
    </Grid>
</UserControl>
